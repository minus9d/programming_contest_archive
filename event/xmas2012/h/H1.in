#include <stdio.h>

int main(void) {
    int i, j;
    int answer = 0;
    for (i = 1; i <= 20121224; ++i) {
        for (j = 1; j <= i; ++j) {
	
            ++answer;
        }
    }
    printf("%d\n", answer);
    return 0;
}
